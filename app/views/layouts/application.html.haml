!!!DOCTYPE html
%html
  %head
    %title= $globals.app
    = stylesheet_link_tag 'application', :media => 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
  %body
    - if current_user
      .section.header
        .content
          .logout= link_to "Log Out", logout_path
    = yield
